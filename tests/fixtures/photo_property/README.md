# Generated photo-property fixture

Tests populate `room_01/` and `room_02/` with deterministic synthetic images at
runtime. The committed directories document the supported property layout without
checking generated binary images into Git. These fixtures test software behavior;
they are not real captures or measurement benchmarks.
